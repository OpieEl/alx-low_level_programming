#include "main.h"

/**
 * main - Check the code
 *
 * Return: 0 Always
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
